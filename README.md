# ECOS Career Corpus Builder

A deterministic desktop utility for extracting resume and cover letter content from a historical application archive into a searchable SQLite database.

## Version

0.1.0

## Current Status

Module 1 - Project Foundation

## Running

Activate the virtual environment.

Install dependencies:

```bash
pip install -r requirements.txt
```

Run:

```bash
python run.py
```